#include<iostream>
using namespace std;
int main(){
    int i=0;
    if(i<3){  // there are answer give only 0 not 0,1,2 because if statement is execute only one time 
    // not many time if contorl statement is not a loop 
        cout<<i;
        i++;
    }
    cout<<endl<<i;
}
